//
// unit tests for src/index.js
//
/* global describe, it */

const chai = require('chai');

const assert = chai.assert;

describe('Index', () => {
  it('should do something', () => {
    assert(true);
  });
});
